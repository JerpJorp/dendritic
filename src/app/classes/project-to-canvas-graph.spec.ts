import { ProjectToCanvasGraph } from './project-to-canvas-graph';

describe('ProjectToCanvasGraph', () => {
  it('should create an instance', () => {
    expect(new ProjectToCanvasGraph()).toBeTruthy();
  });
});
